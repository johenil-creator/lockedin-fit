diff --git a/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Methods_Workout.m b/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Methods_Workout.m
index 1a2b3c4..5d6e7f8 100644
--- a/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Methods_Workout.m
+++ b/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Methods_Workout.m
@@ -139,7 +139,22 @@ - (void)workout_save: (NSDictionary *)input callback: (RCTResponseSenderBlock)ca
         if (!success) {
             NSLog(@"An error occured saving the workout %@. The error was: %@.", workout, error);
             callback(@[RCTMakeError(@"An error occured saving the workout", error, nil)]);
-
             return;
         }
-        callback(@[[NSNull null], [[workout UUID] UUIDString]]);
+        // Also save an ActiveEnergyBurned sample so the Move ring reflects workout calories.
+        // The legacy HKWorkout initializer only sets totalEnergyBurned as workout metadata;
+        // the ring reads HKQuantityTypeIdentifierActiveEnergyBurned samples separately.
+        if (totalEnergyBurned != nil) {
+            HKQuantityType *activeEnergyType = [HKQuantityType quantityTypeForIdentifier:HKQuantityTypeIdentifierActiveEnergyBurned];
+            HKQuantitySample *energySample = [HKQuantitySample quantitySampleWithType:activeEnergyType
+                                                                              quantity:totalEnergyBurned
+                                                                             startDate:startDate
+                                                                               endDate:endDate];
+            [self.healthStore saveObject:energySample withCompletion:^(BOOL energySuccess, NSError *energyError) {
+                if (!energySuccess) {
+                    NSLog(@"Warning: Could not save ActiveEnergyBurned sample: %@", energyError);
+                }
+            }];
+        }
+        callback(@[[NSNull null], [[workout UUID] UUIDString]]);
     };

     [self.healthStore saveObject:workout withCompletion:completion];

diff --git a/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Utils.m b/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Utils.m
index 307056d..23864de 100644
--- a/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Utils.m
+++ b/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Utils.m
@@ -318,6 +318,9 @@ + (HKUnit *)hkUnitFromOptions:(NSDictionary *)options key:(NSString *)key withDe
     if ([unitString isEqualToString:@"calorie"]) {
          theUnit = [HKUnit calorieUnit];
     }
+    if ([unitString isEqualToString:@"kilocalorie"]) {
+         theUnit = [HKUnit kilocalorieUnit];
+    }
     if ([unitString isEqualToString:@"count"]) {
          theUnit = [HKUnit countUnit];
     }
diff --git a/node_modules/react-native-health/app.plugin.js b/node_modules/react-native-health/app.plugin.js
index 8dbb66a..00e1fa4 100644
--- a/node_modules/react-native-health/app.plugin.js
+++ b/node_modules/react-native-health/app.plugin.js
@@ -31,13 +31,13 @@ const withHealthKit = (
   // Add entitlements. These are automatically synced when using EAS build for production apps.
   config = withEntitlementsPlist(config, (config) => {
     config.modResults['com.apple.developer.healthkit'] = true
-    if (
-      !Array.isArray(config.modResults['com.apple.developer.healthkit.access'])
-    ) {
-      config.modResults['com.apple.developer.healthkit.access'] = []
-    }
 
     if (isClinicalDataEnabled) {
+      if (
+        !Array.isArray(config.modResults['com.apple.developer.healthkit.access'])
+      ) {
+        config.modResults['com.apple.developer.healthkit.access'] = []
+      }
       config.modResults['com.apple.developer.healthkit.access'].push(
         'health-records',
       )
diff --git a/node_modules/react-native-health/index.js b/node_modules/react-native-health/index.js
index 06e5105..2c046af 100644
--- a/node_modules/react-native-health/index.js
+++ b/node_modules/react-native-health/index.js
@@ -2,13 +2,14 @@ import { Activities, Observers, Permissions, Units } from './src/constants'
 
 const { AppleHealthKit } = require('react-native').NativeModules
 
-export const HealthKit = Object.assign({}, AppleHealthKit, {
-  Constants: {
-    Activities,
-    Observers,
-    Permissions,
-    Units,
-  },
-})
+const HealthKitObj = Object.create(AppleHealthKit)
+HealthKitObj.Constants = {
+  Activities,
+  Observers,
+  Permissions,
+  Units,
+}
+
+export const HealthKit = HealthKitObj
 
 module.exports = HealthKit
